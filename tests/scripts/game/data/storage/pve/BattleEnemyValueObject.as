package game.data.storage.pve
{
   import game.model.user.hero.HeroEntry;
   import game.model.user.hero.HeroEntrySourceData;
   
   public class BattleEnemyValueObject extends HeroEntry
   {
       
      
      public function BattleEnemyValueObject(param1:HeroEntrySourceData)
      {
         super(null,param1);
      }
   }
}
